# 开发环境配置

# 后端 API 地址（使用相对路径，通过 Vite 代理）
VITE_API_BASE_URL=/api

# 已部署应用访问域名（使用相对路径，通过 Vite 代理）
VITE_DEPLOY_DOMAIN=/api/static

# 预览应用访问域名（使用相对路径，通过 Vite 代理，实现同源以便可视化编辑）
VITE_PREVIEW_DOMAIN=/api/preview